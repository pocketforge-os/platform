#!/usr/bin/env bash
# families/snapdragon/build-bootchain.sh — STUB (B8/M-5). Real behaviour: pass-through signed XBL/TZ/modem/AOP blob group (cannot rebuild signed fw).
# Proves the seam only: the core dispatches the SAME hook name; the snapdragon
# plugin owns all fastboot/XBL/EDL vocabulary (the core never sees it).
set -euo pipefail
echo "[snapdragon/build_bootchain] STUB device=$PF_DEVICE_ID — would: pass-through signed XBL/TZ/modem/AOP blob group (cannot rebuild signed fw)"
echo "[snapdragon/build_bootchain] not implemented in B1 (paper proof). Real onboarding: tsp-1dl.8 / M-5."
exit 0
